{% extends 'layout.njk' %}

{% block content %}
        
<div id="intro">
    <div>
        <p class="strong">As melhores receitas</p>
        <p>Aprenda a construir os melhores pratos com receitas
            criadas por profissionais do mundo inteiro.
        </p>
    </div>
    <div id="chef-container">
        <img src="/assets/chef.png">
    </div>
</div>

<section class="cards">
{% for recipe in data %}
        <div class="card">
            <img src="{{recipe.image}}">
            <p class="card-title">{{recipe.title}}</p>
            <p class="card-title card-author">{{recipe.author}}</p>
        </div>
{% endfor %}
</section>


<script src="/scripts.js"></script>

{% endblock content %}
